{# CTA — Standard (cta-01) #}
{% from "macros/button.njk" import button %}
{% set ctaConfig = sectionConfig or cta or sections.cta %}

<section class="section" data-observe="reveal-up" style="background: var(--surface-container);">
  <div class="container" style="text-align: center; max-width: 640px;">
    <h2 style="margin-bottom: var(--space-4);">{{ ctaConfig.title or ctaTitle }}</h2>
    <p
      style="font-size: var(--text-lg); color: var(--on-surface-variant); margin-bottom: var(--space-8);"
    >
      {{ ctaConfig.description or ctaDescription }}
    </p>
    <div class="flex-center" style="gap: var(--space-4);">
      {{ button(ctaConfig.buttonText or ctaButtonText, { variant: "primary", href: ctaConfig.buttonHref or ctaButtonHref or "#" }) }}
    </div>
  </div>
</section>
